apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: auth-service
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/agifsofyan/tomodachi-infra.git
    targetRevision: main
    path: kubernetes/application/auth-service
  destination:
    server: https://kubernetes.default.svc
    namespace: tomodachi-app
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
