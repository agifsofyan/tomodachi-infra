configs:
  params:
    server.insecure: true

server:
  service:
    type: NodePort # ClusterIP # LoadBalancer

controller:
  replicas: 1

repoServer:
  replicas: 1

applicationSet:
  replicas: 1

redis:
  enabled: true

dex:
  enabled: true

notifications:
  enabled: true